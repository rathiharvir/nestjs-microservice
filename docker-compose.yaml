services:
  nestjs-microservice:
    build:
      context: .
      dockerfile: apps/nestjs-microservice/Dockerfile
      target: development
    command: npm run start:dev
    ports:
      - '3000:3000'
    volumes:
      - .:/usr/src/app
  mongo:
    image: mongo